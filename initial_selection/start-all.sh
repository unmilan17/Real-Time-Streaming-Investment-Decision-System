#!/bin/bash

set -e  # Exit immediately on any error

# ────────────────────────────────────────────────────────────────
# Helper function to run docker-compose silently and filter warnings
# Filters out lines with "level=warning", "WARNING", "deprecated", or "obsolete"
# ────────────────────────────────────────────────────────────────
run_compose() {
  docker-compose "$@" 2>&1 | grep -vE 'level=warning|WARNING|deprecated|obsolete' || true
}

echo "╔════════════════════════════════════════════════════════════════╗"
echo "║     NEWS STREAMING PIPELINE - COMPLETE STARTUP                ║"
echo "╚════════════════════════════════════════════════════════════════╝"
echo ""

# ────────────────────────────────────────────────────────────────
# Pull latest images
# ────────────────────────────────────────────────────────────────
echo "📦 Pulling latest Docker images..."
run_compose pull
echo ""

# ────────────────────────────────────────────────────────────────
# Start MongoDB
# ────────────────────────────────────────────────────────────────
echo "🍃 Starting MongoDB..."
run_compose up -d mongodb
echo "⏳ Waiting for MongoDB to be healthy (20 seconds)..."
sleep 20

if ! docker-compose ps mongodb 2>/dev/null | grep -q "healthy"; then
  echo "❌ MongoDB failed to start!"
  docker-compose logs mongodb 2>&1 | grep -vE 'level=warning|WARNING|deprecated|obsolete'
  exit 1
fi
echo "✅ MongoDB is healthy"
echo ""

# ────────────────────────────────────────────────────────────────
# Start Mongo Express
# ────────────────────────────────────────────────────────────────
echo "🌐 Starting Mongo Express (MongoDB Web UI)..."
run_compose up -d mongo-express
sleep 5
echo "✅ Mongo Express started"
echo ""

# ────────────────────────────────────────────────────────────────
# Start Zookeeper
# ────────────────────────────────────────────────────────────────
echo "🦓 Starting Zookeeper..."
run_compose up -d zookeeper
echo "⏳ Waiting for Zookeeper to be healthy (30 seconds)..."
sleep 30

if ! docker-compose ps zookeeper 2>/dev/null | grep -q "healthy"; then
  echo "❌ Zookeeper failed to start!"
  docker-compose logs zookeeper 2>&1 | grep -vE 'level=warning|WARNING|deprecated|obsolete'
  exit 1
fi
echo "✅ Zookeeper is healthy"
echo ""

# ────────────────────────────────────────────────────────────────
# Start Kafka
# ────────────────────────────────────────────────────────────────
echo "📨 Starting Kafka..."
run_compose up -d kafka
echo "⏳ Waiting for Kafka to be healthy (40 seconds)..."
sleep 40

if ! docker-compose ps kafka 2>/dev/null | grep -q "healthy"; then
  echo "❌ Kafka failed to start!"
  docker-compose logs kafka 2>&1 | grep -vE 'level=warning|WARNING|deprecated|obsolete'
  exit 1
fi
echo "✅ Kafka is healthy"
echo ""

# ────────────────────────────────────────────────────────────────
# Start Kafka UI
# ────────────────────────────────────────────────────────────────
echo "🖥️  Starting Kafka UI..."
run_compose up -d kafka-ui
sleep 5
echo "✅ Kafka UI started"
echo ""

# ────────────────────────────────────────────────────────────────
# Start News Producer (Replay)
# ────────────────────────────────────────────────────────────────
echo "📤 Starting News Producer (Replay)..."
run_compose up -d replay-producer
sleep 5
echo "✅ Producer started"
echo ""

# ────────────────────────────────────────────────────────────────
# Start News Consumer
# ────────────────────────────────────────────────────────────────
echo "📥 Starting News Consumer (with MongoDB)..."
run_compose up -d consumer-stocks
sleep 5
echo "✅ Consumer started"
echo ""

# ────────────────────────────────────────────────────────────────
# Display Summary
# ────────────────────────────────────────────────────────────────
echo "╔════════════════════════════════════════════════════════════════╗"
echo "║                  🎉 ALL SERVICES STARTED! 🎉                   ║"
echo "╚════════════════════════════════════════════════════════════════╝"
echo ""
echo "📊 Service URLs:"
echo "   ├─ Kafka:          localhost:9092"
echo "   ├─ Kafka UI:       http://localhost:8080"
echo "   ├─ MongoDB:        localhost:27017"
echo "   └─ Mongo Express:  http://localhost:8081 (admin/admin)"
echo ""
echo "📂 Output Directories:"
echo "   ├─ CSV/JSON Files: ./output-replay/"
echo "   └─ MongoDB:        news_database.news_collection"
echo ""
echo "📋 Quick Commands:"
echo "   ├─ View all logs:      ./logs-all.sh"
echo "   ├─ View producer:      ./logs-producer.sh"
echo "   ├─ View consumer:      ./logs-consumer.sh"
echo "   ├─ View MongoDB:       ./logs-mongodb.sh"
echo "   ├─ Check status:       ./status.sh"
echo "   └─ Stop everything:    ./stop-all.sh"
echo ""
